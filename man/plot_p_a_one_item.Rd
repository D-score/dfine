% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_p_a_one_item.R
\name{plot_p_a_one_item}
\alias{plot_p_a_one_item}
\title{Plot pass rates by a continuous x for a single item}
\usage{
plot_p_a_one_item(
  pass,
  data_rug = NULL,
  item_var = "item",
  response_var = "response",
  x_var = "agemos",
  by_var = "cohort",
  by_value,
  i = 0,
  min_n = 10,
  model_name = "",
  x.limits = c(0, 60),
  x.breaks = seq(0, 60, 6),
  x.label = "Age (in months)",
  label.trunc = 60,
  col.manual = NULL,
  na.value = "grey"
)
}
\arguments{
\item{pass}{A data frame containing summarized pass rate data, with
variables including \code{item}, \code{p} (percent pass), \code{a} (mean age),
\code{n} (sample size), and \code{cohort}.}

\item{data_rug}{Optional data frame of individual responses to the item,
used to add rug plots. Should include \code{item}, \code{age}/\code{dscore},
\code{response} and \code{cohort}.}

\item{item_var}{\emph{Only relevant for long shape}. Character string with the
name of the column containing item names. The default is \code{item_var = "item"}.}

\item{response_var}{\emph{Only relevant for long shape}. Character string with
the name of the column containing responses. The default is
\code{response_var = "response"}.}

\item{x_var}{Variable representing the continuous x-axis
(e.g., age in months, or D-score).}

\item{by_var}{Variable by which to group the data for plotting.}

\item{by_value}{Value of the item to filter and plot.}

\item{i}{Item index or number (used for annotation).}

\item{min_n}{Minimum number of observations required to include a point.
Defaults to 10.}

\item{model_name}{Optional model name used as annotation in the plot.}

\item{x.limits}{Numeric vector of length 2 specifying x-axis limits
(in months). Defaults to \code{c(0, 60)}.}

\item{x.breaks}{Sequence of breaks for the x-axis. Defaults to
\code{seq(0, 60, 6)}.}

\item{x.label}{Label for the x-axis. Defaults to \code{"Age (in months)"}.}

\item{label.trunc}{Maximum length of the item label to display.}

\item{col.manual}{Optional named vector of manual colors by \code{cohort}.
If \code{NULL}, colors are obtained using \code{get_palette()}.}

\item{na.value}{Color to use for missing values. Default is \code{"grey"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a line plot of the percentage of children passing a specific item
as a function of age, D-score, or other continuous item,
optionally including individual-level responses as rug plots.
}
